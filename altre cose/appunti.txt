/*
	  mentre combatti puoi usare solo le funzioni attacca , inventario e usa

	  attraversa: setCurrentRoom(map *map, player *player, int newRoom)
				  setCurrentRoom: imposta valori della stanza, stampa mappa, imposta nemici, descrivi stanza, descrivi nemici

	  attacca nemico:   	enemyReceiveDamage(enemy *enemy, int 						damage)
	  						playerReceiveDamage(player *player, int damage)
							

	  raccogli oggetto: 	addInventoryPotion
	  						setInventorySword
							setInventoryChestplate

	  inventario: 			showInventory(inventory inventory)

	  usa oggetto:			stampa inventario + usePotion(*player) |												  printMap(*player)

	  attraversa direzione: 
	  aiuto:				
	  esci:					exitGame()
	*/
	